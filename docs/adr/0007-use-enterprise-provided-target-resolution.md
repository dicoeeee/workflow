# Use Enterprise-Provided Target Resolution

Target resolution is provided by enterprise context rather than owned as an internal asset catalog by the Workflow Runtime. Product lines may have incomplete or inconsistent service, component, and repository mappings, so the first version may ship with a provisional resolver that is later reviewed with architects. This keeps the runtime focused on workflow execution while allowing target resolution to mature with enterprise architecture data.
